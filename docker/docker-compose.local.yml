services:
  mini-market-postgres:
    image: postgres:17
    container_name: mini-market-postgres
    ports:
      - "5432:5432"
    env_file:
      - ./../env/.env
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    volumes:
      - ./.volumes/pg_data:/var/lib/postgresql/data
    networks:
      - mini-market-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 10s
      timeout: 5s
      retries: 5

  mini-market-redis:
    image: redis:7.4
    command: redis-server
    container_name: mini-market-redis
    ports:
      - "6379:6379"
    volumes:
      - ./.volumes/redis:/data
    networks:
      - mini-market-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

networks:
  mini-market-network:
    driver: bridge
